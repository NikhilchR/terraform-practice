name: My first programm

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "My first prct"

        - name: list files
          run : ls

        - name: Read file
          run: cat readme.md